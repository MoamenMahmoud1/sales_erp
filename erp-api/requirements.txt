# Core framework
Django==6.1
djangorestframework==3.18.0
adrf==0.1.14
django-filter==26.1
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.30.0

# Database (PostgreSQL via psycopg 3; the `pool` extra enables connection pooling)
psycopg[binary,pool]==3.3.4

# ASGI deployment
gunicorn==26.2.0
uvicorn[standard]==0.52.4
uvicorn-worker==0.4.0

# Auth / sessions / email / uploads
django-cors-headers==4.9.0
django-phonenumber-field[phonenumbers]==8.5.0
Pillow==12.3.0

# Storage / caching (production)
django-storages==1.14.6
django-redis==7.0.0
boto3==1.43.55
redis==8.0.1

# Configuration
python-decouple==3.8

# Development / profiling tooling (not added to INSTALLED_APPS in production)
django-extensions==4.1
django-silk==5.5.0

# Static analysis / formatting
ruff==0.16.5
